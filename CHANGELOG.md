# 更新日志

所有重要的变更都会记录在此文件中。

---

## [26.9.91] - 2026-09-09

### Added

#### 🔌 插件化与拓展体系

- ✨ 新增 Solon H-SPI 拓展包（ExtPack）体系：设置页新增「拓展包」管理区块，粘贴 jar 直链或本地路径即可安装，支持启用/停用（立即生效）与卸载，安装目录为 `~/.loopra/extpacks`；同时新增 `loopra-extra-demo` 示例工程，演示 bridge / 插件 / 工具 / 控制器的完整扩展写法并附带冒烟测试
- ✨ 新增 Provider 请求体拦截器机制：拦截器可在模型请求发出前改写请求体（覆盖 chat_completions / responses / anthropic 协议），并重构为全局注册模式；内置 DeepSeek、Mimo 等渠道特化拦截器
- ✨ 模型调用失败自动记录错误日志：HTTP 日志与原始响应日志插件补充请求失败场景的状态码、响应与异常信息，便于排障

#### 🤖 模型渠道与兼容

- ✨ 模型条目新增 `maxTokens` 配置（单次请求最大输出 token 数，含推理 token），请求前自动注入：Chat Completions 映射 `max_tokens`，Responses 映射 `max_output_tokens`，未配置时默认 32768
- ✨ 渠道新增「特殊兼容」配置：选择 `opencode` 后由 OpenCode 兼容插件自动携带稳定的 `x-opencode-session` 会话路由头，适用于 OpenCode Zen / Go 兼容网关；旧版全局特殊兼容与输出上限配置自动迁移到渠道/模型条目
- ✨ 支持独立的图片理解模型回退：当前模型不支持图片输入时，可在模型渠道页配置 `imageUnderstandingModel`（及所属渠道），由其将 `read_image` 的图片转换为文字描述后再交给当前模型；用户上传的图片会先保存到项目 `.loopra/read_img/`，再以相对路径与识别提示交给模型按需读取

#### 🔗 MCP 服务与项目集成

- ✨ 支持将 Loopra 发布为 MCP Server 对外提供工具：设置页「MCP 服务器 → 发布 Loopra 工具」开启后，以 Streamable HTTP（默认 `/mcp`，可切换 SSE）对外服务，可发布全部已启用工具或手动白名单选择，配置保存于 `~/.loopra/mcp-server.json`
- ✨ 新增 Cloudflare Quick Tunnel 内网穿透：由 Java 进程直接管理本机 `cloudflared`，一键生成临时公网地址，且只转发已配置的 MCP endpoint、不暴露其它 Web 路由，Windows / macOS / Linux 通用（Quick Tunnel 仅支持 Streamable HTTP）
- ✨ MCP 导出携带工具安全注解（只读/写入语义），并修复外部 MCP 客户端连接的 CORS 与错误响应兼容问题
- ✨ 项目级 MCP 服务器与技能：项目根目录 `.loopra/mcp-servers.json` 会被自动读取并只对当前项目生效（兼容 `mcpServers` 对象与 `servers` 数组两种格式）；桌面端会话左侧新增「项目能力」面板展示当前项目 Skill、MCP 连接状态与工具列表，修改项目 MCP 配置后可一键刷新，Agent 也可调用 `mcprefresh` 重建连接

#### 🧩 子代理会话管理

- ✨ 子代理执行过程完整落盘并可回放：每个子代理会话独立以 JSONL 记录任务描述、开始/结束状态及逐段推理与工具事件；桌面端左侧活动栏新增「子代理会话」面板，列出当前会话的历史子代理记录（运行中实时刷新），双击打开独立回放标签查看完整执行过程，并支持继续对话与删除记录
- ✨ 新增子代理会话 API：`GET /api/sub-sessions` 列表与 `GET /api/sub-sessions/{subSessionId}/events` 事件回放，会话删除/清空时级联清理子代理记录

#### 💬 聊天与会话

- ✨ 会话模型设置迁移到服务端持久化：会话的模型、渠道与推理强度按「请求值 → 会话元数据 → Agent 缓存 → 全局默认」顺序解析并回写会话元数据，旧版浏览器本地缓存自动迁移到服务端后清除
- ✨ 超大粘贴内容自动外置：输入文本超过 32 KiB 时自动保存为项目内 `.loopra/paste/` 参考文件并以相对路径引用（服务端校验目录边界与符号链接，单次上限 4 MiB），模型按需调用 `read` 读取，不再整段进入消息
- ✨ 助手回合过程摘要：最终回答之前的思考、工具调用与中间正文自动收进消息顶部「用时 X」的可折叠摘要块（尚未产出回答时实时占位），长回合浏览更聚焦
- ✨ 消息块折叠/展开加入平滑过渡动画（覆盖推理、工具详情、加密思考、子代理、Checklist、Goal 等折叠块）

#### 🖥️ 桌面端

- ✨ 桌面端浮层与标题栏升级：首页/会话标签/探索等菜单与重命名、确认对话框改用原生浮层（overlay）统一承载，Windows 标题栏改为原生窗口控件并随主题与模态状态自动调色（macOS 隐藏式标题栏）；新增全局搜索浮层（跨会话搜索聊天并执行快捷操作），AI 浏览器交互重构
- ✨ 左侧边栏支持拖拽调整宽度并持久化
- ✨ 统一桌面端消息提示层：各页面成功/失败提示统一为桌面内联消息组件，适配主题与多场景（含更新、设置页保存等）
- ✨ 改进欢迎页与项目「新增对话」交互：欢迎页可直接发起会话，项目入口进入对话的流程更顺畅
- ✨ 重构工具管理页：搜索、分类与启用/停用交互全面打磨，并与桌面端其它视图样式统一

### Changed

- 🔄 前端布局与视觉样式统一精修：新增 `accent-btn` 主按钮颜色变量并统一各页面按钮配色，调整桌面首页搜索框位置与整体间距
- 🔄 移除旧的全局输出上限与特殊兼容字段，统一收敛到渠道与模型条目（旧配置自动迁移）

### Fixed

- 🐛 修复暗色主题下代码块对比度不足的问题
- 🐛 忽略模型思考间隙产生的空白正文块，避免空内容被渲染为正式回复正文
- 🐛 修正瞬态模型错误的判定优先级，瞬时网络故障不再被误判为配置/鉴权错误
- 🐛 修复 cutin 视图外部工具被同步清空、重复注册时新实例不生效的问题，并修复插件停止时路由卸载与事件注销逻辑
- 🐛 修复更新流程：更新前先停止旧核心服务进程，更新完成后自动重启
- 🐛 修复引导流程中排队消息未等旧生成流关闭即被发送的问题
- 🐛 过滤空的技能提供者，避免空指针异常
- 🐛 npm 发布遇到相同版本号时不再报错
- 🐛 子代理记录删除确认统一改为系统对话框

### Chore

- 🔧 JUnit 临时目录不再写入项目注册表，避免测试目录残留污染首页项目列表

---

## [26.8.243] - 2026-08-24

### Added

#### 🚀 安装、启动与更新

- ✨ 新增 npm 发布流程（OIDC trusted publishing）与 `loopra-dist` 发布包：分发包原样收纳 GitHub Releases 上的 `loopra-dist.tar.gz`，可从 npm registry（含 npmmirror 等国内镜像）拉取，作为 GitHub 直连/镜像之外的备用下载源
- ✨ `loopra-dist` 支持 postinstall 自动安装：`npm install -g loopra-dist` 即解压分发包并按当前平台自动执行包内安装器（Windows → `install.ps1`，macOS/Linux → `install.sh`），等效于官方一键安装脚本；不需要自动安装时可用 `npm install --ignore-scripts` 跳过
- ✨ npm 安装镜像自动测速：未设置 `LOOPRA_MIRROR` 时并发 HEAD 探测候选代理（`gh-proxy.org` / `ghfast.top` / `gh-proxy.com` / `ghproxy.net`）多轮取中位延迟，自动选用最快者（GitHub 直连 HEAD 延迟不代表实际下载可用性，不参与比拼），全部探测失败回退直连；测速与安装每一步进度记录到 `~/.loopra/install.log`

#### 💬 界面与交互

- ✨ 桌面端新增中文字体切换：设置页可枚举系统字体并选择，英文/数字统一使用 JetBrains Mono Variable；UI 设置持久化到 `userData/ui-settings.json`（file:// 页面 localStorage 在 Electron 中不可靠，重启会丢失）
- ✨ 路径组折叠头按工具类型分类统计（读 / 写 / 执行 / 记忆 / 子代理等）
- ✨ Dashboard 重构为年度用量视图：近一年 GitHub 风格热力图 + 月度标签，默认滚动定位到最新数据
- ✨ 模型渠道设置页改为侧边栏 + 详情主从布局

### Changed

- 🔄 Solon 升级至 4.0.6（含 solon-ai 4.0.6）
- 🔄 会话列表加载不再为每个项目创建 Agent：仅读取会话目录元数据，优先独立会话存储，仅当会话已有缓存 Agent 时复用其存储（此前桌面端每次加载会话列表都会为每个项目新建 `default` 会话 Agent，每个 Agent 含 21 个插件实例，跨项目切换时插件实例只增不减）
- 🔄 切换项目时顺带清理旧项目空闲的 `default` 会话 Agent 缓存（运行中的会话跳过，防止误中断正在执行的回合）
- 🔄 Electron 标签管理全面空安全：`webContents` 判空与 `isDestroyed()` 前置检查，避免销毁竞态

### Fixed

- 🐛 修复推理摘要重复渲染；ReasonBreaker 检测到思考循环重试时，按服务端 `stream_reset` 回滚到本轮第一个思考块之前（保留工具卡片、choice、子代理容器等既有内容）
- 🐛 修复 Windows 下命令输出中文乱码：采用 UTF-8 优先、系统活动代码页兜底的自适应解码（git/node/python 等向管道输出 UTF-8 字节，cmd 内置命令按 OEM 代码页输出 GBK 字节，固定任一编码都会导致另一半场景乱码）
- 🐛 修复模型渠道设置页布局：侧边栏固定高度、渠道列表内部滚动，行内模型卡片防塌缩（补 `min-height` 防止文字被裁剪）
- 🐛 修复热力图列宽弹性自适应
- 🐛 npm publish 升级 Node 24 并显式 `--access public`，修复 trusted publishing 发布失败
- 🐛 build.yml 补充显式 checkout，修复 npm 发布步骤找不到 `.release/npm/loopra-dist` 的问题
- 🐛 npm 安装器绕过 npm 对 stdout 的捕获（进度与测速结果直接写控制台设备 `CONOUT$` / `/dev/tty`），默认 `npm install` 也能看到实时进度和测速结果；非交互环境（CI/管道）跳过询问默认直连；交互环境镜像菜单 30 秒未选择则回退直连

### Chore

- 🔧 bump-version 脚本支持同步 `loopra-dist` 的 `package.json` 版本

---

## [26.8.231] - 2026-08-23

### Added

#### 🔌 插件体系

- ✨ 外置插件支持一行命令安装：`/plugin add` 同时接受远程 jar 直链与本地 jar 路径

#### 🚀 安装、启动与更新

- ✨ 启动流程支持 Java 环境检测与 JRE 本地安装/下载：启动窗口可检测系统 Java，并支持选择镜像安装/重装 JRE 25
- ✨ 新增镜像源支持与前端更新链路优化：下载源支持镜像列表自动测速并按延迟排序、可一键重测；所有安装脚本与更新脚本支持 `LOOPRA_MIRROR` 指定镜像，选中镜像同时用于核心包与 JRE 下载
- ✨ 安装脚本 JDK 预检调整为 Solon 风格三级策略：优先系统 Java 17+，其次复用已有捆绑 JRE（`~/.loopra/jre25`），均缺失时经 Adoptium 自动下载最新 JRE 25（GitHub 直连或镜像加速）

#### 💬 界面与交互

- ✨ 环境面板新增 AI 生成提交消息功能
- ✨ 技能市场新增「已安装技能」折叠面板，支持查看、刷新与卸载本机技能；桌面宠物新增「重置位置」按钮
- ✨ 聊天页新增一键折叠所有展开块的按钮，折叠全部消息块与子代理/工具/代码块展开状态

### Changed

- 🔄 前端 Markdown 渲染迁移至 markdown-it 并改用 Shiki 语法高亮
- 🔄 移除引导页中的会话迁移功能

### Fixed

- 🐛 修复虚拟滚动下强制跳底后底部对齐不准确的问题

---

## [26.8.211] - 2026-08-21

### Added

#### 🔌 插件化内核与运行时

- ✨ 引入 cutin 插件化核心框架重构 Loopra 内核：模型 Provider、工具注册表与循环引擎全部插件化；`cutin-core` 模块更名为 `cutin`，`loopra-model` / `loopra-harness` / `loopra-acp` 整合为单模块 `loopra`
- ✨ 插件注册支持可注销句柄与热插拔，插件可安全卸载
- ✨ 新增插件运行时管理与设置页，可查看、启用/禁用插件
- ✨ 插件化提示词组装与工具网关，支持外部插件注入提示词切片与自定义工具，并新增工具协作约定唯一来源 `ToolContract`
- ✨ 新增 HTTP 日志插件与原始响应日志插件，模型响应附带原始请求/响应体并记录日志

#### 🤖 模型与推理

- ✨ 支持 OpenAI reasoning 内容提取与透传
- ✨ 支持多模态图片输入，重构 AgentLoop preflight 流程
- ✨ 新增 `xhigh` 推理强度
- ✨ 加密思考占位符与推理开始阶段支持

#### 📊 Token 用量与速度

- ✨ 新增 token 生成速度展示（实时与平均），含会话亲和优化

#### 💬 聊天与会话

- ✨ 支持本轮关联多个项目作为联动上下文
- ✨ 支持项目和会话的多选批量删除
- ✨ 支持会话重命名，会话列表显示时间字段

#### 🖥️ 桌面端

- ✨ 新增环境面板与桌面端 Git 操作，刷新按钮独立手动加载
- ✨ 桌面文件树改 VS Code 风格——单击仅选中、双击打开编辑器标签并移除预览弹窗
- ✨ 新增首次使用引导窗口

### Changed

- 🔄 用 Snack4 替换 Jackson 并支持工作目录
- 🔄 统一项目与隔离分支术语，引入会话环境抽象
- 🔄 移除运行时冗余抽象与未使用代码；简化用量栏样式并移除刷新按钮
- 🔄 LoopNode / LoopEdge 增加 `remark` 字段，LoopProgram 支持 prettyPrint
- 🔄 同步 cutin 重构后的打包、版本与 CI 产物路径

### Fixed

- 🐛 修复模型注册表热更新后无法解析新模型的问题
- 🐛 修复停止生成可能无效的问题（LRU 淘汰跳过运行中 Agent、渠道切换前中止旧 Agent、未命中时兜底中止全部运行中 Agent）
- 🐛 修复 Chat Completions 流式 usage 累加导致的 token 统计放大（OpenRouter 多块）
- 🐛 修复 BEFORE_MODEL 折叠异常导致前端 token 一直为 0
- 🐛 修正会话开始回调时机并移除冗余的用户消息状态
- 🐛 统一处理流式模型错误并增强瞬时异常识别，修复瞬态错误误判
- 🐛 每次模型调用后重置重试预算
- 🐛 过滤模型可见工具模式中的运行时参数
- 🐛 新增消息修复插件以兼容模型 API 约束
- 🐛 优化 `/continue` 命令在无 HITL 时直接基于现有上下文继续推理
- 🐛 兼容 llama.cpp timings 用量统计
- 🐛 修复引导流程会话迁移与配置保存问题、环境信息页面布局问题
- 🐛 修复 AGENTS / CLAUDE 规则迁移目标并实际注入项目规则
- 🐛 计划评审条带宽度限制为 1040px 并居中

### Chore

- 🔧 清理临时截图脚本与 IDE 配置

---

## [26.8.131] - 2026-08-13

### Added

#### 🤖 模型渠道与协议

- ✨ 新增 Anthropic Messages API 协议支持，渠道 `apiProtocol` 可选 `anthropic`（`x-api-key` + `anthropic-version` 认证），支持流式输出、token 用量与缓存统计
- ✨ 支持 Anthropic `thinking` / `redacted_thinking` 块的原样回传，保留扩展思考内容
- ✨ 新增 OpenAI 快速模式开关（`service_tier=fast`），按会话启用，仅 OpenAI 协议（chat_completions / responses）生效

#### 💬 聊天与输入

- ✨ 支持带行号的代码片段引用并去重，提升上下文引用效率

#### 🖥️ 桌面端

- ✨ 新增 bash 会话日志查看功能，并修复 Windows 下会话日志编码问题
- ✨ 支持将核心运行时嵌入安装包，实现无网络离线安装
- ✨ 文件图标改用 codicon 替代 Seti 字体，文件按钮与版本管理图标改用内联 SVG

### Changed

- 🔄 macOS 构建支持代码签名与公证（配置 secrets 后自动生效）；未配置证书时改用 ad-hoc 签名并更新发布说明
- 🔄 标题栏分隔线从左侧 50px 改为全宽

### Fixed

- 🐛 移除 codicon.css 中 URL 查询参数，修复 Electron 字体加载失败
- 🐛 并发消费 Git 输出流，避免管道死锁
- 🐛 electron-builder 打包步骤指定 `shell: bash`，修复 Windows runner 上 PowerShell 解析失败导致的打包失败

---

## [26.8.122] - 2026-08-12

### Added

#### 📁 桌面文件资源管理器

- ✨ 新增桌面端文件资源管理器，支持打开本地文件夹、目录树浏览与 Seti 文件类型图标
- ✨ 文件树实时监听磁盘变化自动刷新，保留已展开目录并同步刷新 Git 状态装饰
- ✨ 支持将文件拖拽到聊天输入框作为上下文，右键菜单可添加到对话
- ✨ 左侧文件面板支持拖拽调整宽度并持久化；文件删除前增加确认对话框

#### 📝 文件编辑器

- ✨ 新增基于 Monaco 的文件编辑器，多标签页编辑与保存、关闭未保存确认、视图状态恢复
- ✨ 支持 Git 脏文件差异对比：修改行蓝色、新增行绿色、删除行红色标记，含内容防抖与大文件保护

#### 🖥️ 终端面板

- ✨ 新增终端面板及 node-pty 集成，支持选择系统可用 Shell
- ✨ 终端支持垂直模式并作为右侧独立面板展示，可拖拽调整高度与宽度并持久化
- ✨ Agent 调用 `bash_start` 时自动展开命令页签

#### 🗂️ 桌面聊天标签与活动栏

- ✨ 桌面聊天页新增左侧活动栏，一键切换文件面板与版本管理面板
- ✨ 桌面端新增更新入口，优化标签中键关闭行为
- ✨ 桌面聊天标签事件统一投递并移除 CDN 依赖

#### 💬 聊天与欢迎页

- ✨ 支持上传/粘贴文件解析为上下文
- ✨ 聊天消息与输入区宽度自适应优化，助手消息铺满内容列
- ✨ 欢迎页以 Loopra 品牌标识替换动态问候语，进入会话自动收起并保持左侧文件栏折叠状态

#### 🤖 Git

- ✨ AI 生成提交消息支持全部文件名与部分 diff 优化

### Changed

- 🔄 移除会话任务状态栏，合并流式与后台执行指示器
- 🔄 更新前端依赖版本

### Fixed

- 🐛 修复文件树箭头图标在加载后变大问题
- 🐛 修复终端面板主题同步与展开/收起时的多个体验问题
- 🐛 修复流式输出时历史加载覆盖消息数组的问题

---

## [26.8.91] - 2026-08-09

### Added

#### 📋 需求池（Requirement Board）

- ✨ 新增需求池看板，按待执行、执行中、已完成和已失败展示需求；支持设置项目、优先级、需求描述与验收标准
- ✨ 支持立即执行和定时执行；每条需求可单独选择模型渠道、推理强度和审批模式
- ✨ AI 为每条需求使用独立执行会话，持续展示执行日志、工具耗时、完成总结和失败原因
- ✨ 支持执行期间发表评论，AI 可在需求专属会话中回复；支持人工停止执行并将需求退回待执行状态
- ✨ 新增 `show_requirements`、`finish_requirement` 和 `reply_requirement_comment` 需求执行工具，需求数据持久化到 `~/.loopra/requirements/requirements.json`

#### 🖥️ 桌面端交互

- ✨ 新增桌面首页右键菜单，可打开需求池或切换主题
- ✨ 新增会话标签右键菜单，支持刷新、关闭当前标签以及关闭左右侧标签
- ✨ 项目和会话支持右键上下文操作，需求池可从桌面首页和应用菜单打开

#### 💬 会话运行状态

- ✨ 新增会话后台运行状态感知；重新打开或切换到仍在执行的会话时，输入区会自动锁定并保留停止入口
- ✨ 新增 `GET /api/agent/session-status` 会话状态查询接口，运行期间自动刷新历史消息，停止后执行最终同步

### Fixed

- 🐛 修复桌面端重启后遗留核心服务进程未清理、可能继续复用旧版本服务的问题
- 🐛 修复桌面端隐藏宠物时误隐藏聊天内宠物的问题，并持久化宠物隐藏状态
- 🐛 修复无边框窗口下部分按钮无法点击的问题
- 🐛 修复 `AskChoiceTool` 对非 `Map` 类型选项的转换兼容性
- 🐛 完善需求池评论交互、执行日志展示和审批处理

### Chore

- 🔧 将前端构建产物同步纳入版本库，更新官网静态页样式与国际化资源，并更新微信图标

---

## [26.8.40] - 2026-08-04

### Added

#### 📋 计划模式

- ✨ 计划模式 UI 化：输入框新增计划模式按钮与计划审查面板，集成 `agent/mode` 接口，实现只读探索 → 待审计划展示 → 批准执行的完整闭环；移除 `/plan`、`/execute` 斜杠命令
- ✨ 新增 `submit_plan` 工具：计划模式下提交执行计划（步骤、概述、风险），经 `plan_submitted` 事件推送前端；用户批准后自动退出计划模式并按计划执行
- ✨ AgentLoop 支持计划模式状态管理与计划注入，会话持久化（`JsonlSessionStore`）同步增强

#### 🖥️ 桌面更新与启动体验

- ✨ 新增启动窗口（DesktopSplash）与更新窗口（DesktopUpdate），版本更新流程独立成窗
- ✨ 新增下载源选择：直连（GitHub 官方发布）或镜像（gh-proxy 加速），选择本地持久化，更新命令随所选源切换
- ✨ 标题栏新增版本更新检查与提醒按钮；更新面板拆分“更新核心服务”与“更新桌面端”，检测到桌面端新版本时高亮提示
- ✨ 重构桌面首页布局并统一外部链接管理；首页左下角菜单新增子代理入口
- ✨ 桌面宠物右键菜单新增“关闭宠物”（带警告确认）；宠物窗口与主窗口均可关闭宠物，关闭后主窗口同步隐藏状态

#### 🧩 子代理与前端

- ✨ 子代理配置支持前端编辑、持久化与同步策略
- ✨ 子代理支持选择渠道模型，变更时静默保存
- ✨ 前端支持数学公式渲染（KaTeX），`$...$` 行内公式与 `$$...$$` 块级公式

#### 🔌 内核与扩展

- ✨ `browser_screenshot` 按当前模型 `imageInput` 能力自适应附带截图
- ✨ `loopra-model` 压薄为纯内核，编排设施下沉 `loopra-harness` 并以 SPI 倒置（`ToolScanProvider`、`AgentConfig`、`GoalGuard`、`ToolPolicyProvider`、`SessionUsageSink`）
- ✨ `LoopraAgent` 开放 `goalGuard` / `toolPolicyProvider` / `sessionStore` 注入点，支持上层定制

### Changed

- 🔄 计划模式交互由斜杠命令迁移至输入框按钮与计划审查面板
- 🔄 最新版本检查地址由 GitHub 切换至 Gitee Releases
- 🔄 代码注释统一调整为中文并修复缩进

### Fixed

- 🐛 修复更新窗口内嵌布局
- 🐛 修复桌面宠物窗口关闭后主窗口宠物状态不同步的问题

### Chore

- 🔧 清理 Loopra 配置目录，更新 `.gitignore`
- 📚 新增文档：模块化改造全程沉淀（`docs/loopra-modular-refactor.md`）、LoopraAgent 定制接口改造记录（`docs/loopra-agent-customization.md`）

---

## [26.8.3.2] - 2026-08-03

### Added

#### 🛠️ OpenAPI 与安装发布

- ✨ 新增 `OpenApiV2Resolver` 解析器，支持 Swagger 2.0（OpenAPI V2）规范解析 API 工具；数组 items 与对象属性中的循环引用可自动退出并标记 `_Circular_Reference_`，避免无限递归
- ✨ 新增 Gitee 镜像安装脚本 `setup-mirror` / `setup-gui-mirror`（Windows PowerShell / macOS / Linux），国内网络可从 Gitee 直接下载安装包
- ✨ 新增 `bump-version.ps1` 版本升级辅助脚本

#### 🖥️ 桌面工作台与打包

- ✨ DesktopHome 支持工作区（项目）拖拽排序，实时预览并持久化到服务端配置，读取失败时回退默认顺序
- ✨ 新增 Electron 打包流水线，支持构建 macOS 与 Windows 平台的桌面安装包

### Changed

- 🔄 **模块化重构**：原 `loopra-bin` 拆分为三个可独立发布的 Maven 模块 —— `loopra-model`（内核：ChatModel 客户端/协议、Agent 推理循环、工具基础抽象、会话/配置/Goal/Checklist/工作区）、`loopra-harness`（内置工具与 MCP/LSP/OpenAPI/Skill 技能桥接、定时任务）、`loopra-acp`（将 Agent 注册为 ACP Agent）；`loopra-web` 聚合上述模块，功能不变
- 🔄 内核与上层解耦：工具扫描改为 `ToolScanProvider` SPI（harness 启动时安装），图片结果改由 `ImageToolResult` 文本协议传递，父代理输出经 `ParentOutputHolder` 线程桥接，`loopra-model` 不再依赖任何上层模块
- 🔄 工具校验模型自身故障（超时、调用异常等）时不再直接判定拒绝，改为回退人工审批由用户决定是否执行
- 🔄 安装脚本与 README 下载地址从 Gitee 迁移至 GitHub 发布源，国内用户可选用新增的 Gitee 镜像脚本
- 🔄 桌面端将后端返回的相对资源路径统一解析为完整 URL（`resolveApiUrl`），修复 `file://` 协议下宠物 spritesheet 加载失败
- 🔄 CI 全面迁移至 GitHub Actions 构建与发布流水线，Release 只上传最终安装包，剔除 unpacked 解包目录
- 🔄 更新 solon-parent 至 4.0.4，版本号统一升级至 26.8.3.2

### Fixed

- 🐛 修复 GitHub Actions 构建失败并消除 Node 20 弃用警告
- 🐛 修复连续审批时误删上一轮已完成工具卡片的问题

### Chore

- 🔧 删除临时脚本 `fix2.cjs` 与 `fix-app.cjs`

---

## [26.8.3] - 2026-08-03

### Added

#### 🎯 Goal 目标管理

- ✨ 强化 Goal 生命周期管理与并发安全持久化，支持步骤状态、暂停、恢复、阻塞和完成等状态流转
- ✨ 新增 Goal 步骤前端展示与轮询，聊天输入区域可查看当前目标进度
- ✨ 新增 `/goal reset`，Goal 快照损坏时可清除状态并恢复会话

#### 🖼️ 多模态与浏览器

- ✨ 新增 `read_image` 工具，支持工作区路径、绝对路径、Base64/data URI 和 HTTP(S) URL；支持 `auto`、`low`、`high` 详情级别，单张图片最大 5 MiB
- ✨ 增强 `browser_screenshot`，同时返回可见视口截图、结构化页面快照、可操作元素、遮挡状态、弹层和提示信息，并通过 `snapshotId` 防止使用过期页面状态

#### 💬 聊天与桌面体验

- ✨ 生成期间发送的消息会进入队列，支持折叠摘要、移除排队消息，以及停止当前生成后引导发送
- ✨ 新增服务端持久化的“常用要求”预设，可添加、编辑和删除；点击预设会直接追加到输入框
- ✨ 桌面宠物新增 AI 回复气泡、连续缩放、状态与位置恢复、点击激活主窗口及离线静默请求
- ✨ 首次使用且尚未配置模型渠道时，新增模型配置引导提示

### Changed

- 🔄 Desktop 启动时优先复用本机 `4567` 端口上的健康 Loopra Web 服务；独立桌面运行时版本不一致时可更新或暂不更新
- 🔄 Responses API 将 reasoning 提升为消息级内容，回放时移除输出专用字段；遇到可恢复的 `invalid_request_error` 时自动回退最近的 reasoning assistant 回合并重试一次
- 🔄 移除独立的视觉模型配置和 `vision_recognize` 入口，图片读取改由当前模型的 `imageInput` 能力决定是否可用
- 🔄 优化 Desktop 多聊天标签页、窗口聚焦、输入框焦点恢复和模型渠道管理交互

### Fixed

- 🐛 捕获 SSE 畸形 JSON、空数据块和流式错误，统一进入明确的重试或错误回调路径
- 🐛 完善停止生成链路，覆盖请求登记竞态、后台任务、Agent/HTTP 调用和 SSE emitter，修复停止聊天时连接提前关闭的问题
- 🐛 补齐常用要求预设接口的路由映射，修复预设接口未注册导致无法加载或保存的问题

---

## [26.7.24.1] - 2026-07-24

### Added

#### 🚀 首次启动与桌面运行时

- ✨ 首次启动时自动检测未配置的模型渠道，并跳转至模型渠道设置；保存后自动重新加载并初始化 Agent
- ✨ Desktop 使用独立运行时目录 `~/.loopra-gui`，与 CLI 安装目录和已有服务相互隔离
- ✨ 新增 Desktop 运行时在线安装脚本，安装器以显式参数区分 GUI 与 CLI 安装模式

### Changed

- 🔄 分离 Desktop 与 CLI 的安装、进程管理和卸载流程；Desktop 只发现、启动和终止自己的运行时，配置仍保存在 `~/.loopra`
- 🔄 调整官网为轻量设计系统，并精简 README 的安装、配置和开发说明
- 🔄 上下文折叠的 prompt token 估算改为采用本地估算与服务端实际用量中的较大值

### Fixed

- 🐛 识别 Chat Completions 与 Responses API 的上下文超限错误，并自动折叠上下文后恢复执行
- 🐛 修复 macOS 上 `ps` 截断长命令行导致 Desktop 无法正确识别 Java 服务进程的问题

### Chore

- 🔧 升级版本至 v26.7.24.1，并同步更新安装脚本、构建元数据和前端静态资源

---

## [26.7.23] - 2026-07-23

### Added

#### 🤖 模型渠道与协议适配

- ✨ 新增模型渠道管理 — 每个渠道独立配置 API 地址、密钥、协议及模型能力，支持设置默认模型
- ✨ 支持 OpenAI Responses API — 完成请求、流式响应、工具调用和推理内容的协议适配
- ✨ 支持自定义推理强度及模型级上下文窗口、图片输入和价格配置
- ✨ 添加独立校验模型 — 可在人工审批前评估危险工具调用，保留工作区越界的强制人工审批
- ✨ 新增用户标识和会话缓存键注入 — 提高渠道侧请求隔离与前缀缓存命中

#### 🛠️ 工具、协作与项目记忆

- ✨ 新增工具管理界面 — 可查看工具元数据并自定义只读/写入执行权限分类
- ✨ 新增项目级长期记忆 — 通过 `memory` 工具将可复用事实持久化到工作区，支持检索、追加、列举和删除
- ✨ 共享工作区按项目持久化 — `workspace_*` 条目保存至 `.loopra/workspace/`，支持跨会话和父子代理协作
- ✨ 新增子代理管理页面并统一工具分类展示
- ✨ 工具调用自动注入工作目录 `__cwd`，并豁免 `bash_wait` 等轮询工具的风暴检测

#### 🖥️ 桌面端与聊天体验

- ✨ 集成可见的 AI 浏览器 — 支持标签页、结构化页面快照、受控交互及用户接管登录/验证
- ✨ 支持桌面端多聊天标签页、原生元素检查窗口和后端服务进程管理
- ✨ 新增点击 AI 回复中的本地文件路径预览、差异块导航与选中状态
- ✨ 实现聊天消息虚拟滚动、项目/会话刷新和数据面板今日筛选
- ✨ 新增模型选择器默认模型设置、渠道折叠展示及多项下拉交互优化
- ✨ 桌面宠物支持交互穿透，所有窗口统一应用图标与 Windows App User Model ID

### Changed

- 🔄 将模型 API 请求与响应逻辑抽取为协议策略，统一重试、取消和日志处理
- 🔄 模型配置由字符串列表演进为带能力与价格信息的对象，兼容旧配置
- 🔄 子代理冻结会话与工具前缀并保持缓存亲和；文件变更由父代理统一提取
- 🔄 优化 Git 面板的多渠道模型选择、模型设置布局、元素面板和滚动条样式
- 🔄 移除已废弃的 `loopra-tui` 终端界面代码，保留 CLI、Web 和 Desktop 三端入口
- 🔄 CI 流水线改为 Maven 构建并同步前端静态资源，增加 Maven Central 仓库配置

### Fixed

- 🐛 修复 HTTP 重试异常、错误响应体读取、请求头/参数注入及聊天 API 地址拼接问题
- 🐛 修复 Jsonl 会话并发写入与 flush、会话头标识、工作区切换和初始化边界问题
- 🐛 修复共享工作区写入、序列化和项目隔离问题，补齐相关回归测试
- 🐛 修复流式 SSE 空数据块、工具路径终态、DeepSeek-R1 思考标签及风暴断路器误判
- 🐛 改进图片预览无障碍交互，修复深色模式按钮和 Vue Router 兼容性问题

### Chore

- 🔧 升级版本至 v26.7.23

---

## [26.7.16] - 2026-07-16

### Added

#### 📋 清单系统（原工作流）

- ✨ 将工作流概念重命名为清单 (Checklist) — 概念更清晰，与开发习惯一致
- ✨ 清单进度展示为悬浮浮层 — 不占用主界面空间，悬停即可查看
- ✨ 重构清单 UI 为步骤视图 — 直观展示当前步骤、已完成和待办

#### 🔄 子代理系统重构

- ✨ 重构 task 工具为预设角色 sub_agent — `explore` / `implement` / `test` / `review` / `plan` 五种角色，各有只读/读写权限约束
- ✨ 子代理显示方式从弹窗改为内嵌 — 执行过程在聊天流中直接展开，不用跳窗
- ✨ 重构子代理系统 — 改进中断处理、配置继承和事件标识
- ✨ 补充 sub_agent 角色分工与协作指引 — 派发时自动注入角色约束和汇报格式
- ✨ 支持子代理 HITL 审批与事件路由 — 子代理中的写操作同样触发人工审批
- ✨ 添加风暴断路器与子代理超时取消机制 — 防止子代理陷入死循环或超时

#### 🔒 HITL 审批增强

- ✨ HITL 模式扩展为三态 — `free`（自由模式）/ `approval`（审批模式）/ `auto`（自动放行模式）
- ✨ 实现工具自动放行白名单机制 — 纯交互控制流工具（如 `finish`、`ask_choice`）直接放行
- ✨ 添加系统提示词编辑功能及优化 HITL 审批展示 — 审批卡片展示更清晰

#### 📝 文件变更追踪与撤销

- ✨ 显示本轮 AI 编辑的文件变更摘要 — 每次回复后自动展示修改/新增/删除统计
- ✨ 文件变更持久化与展示优化 — 变更记录持久保存，支持在消息中回溯
- ✨ 添加撤销 AI 文件修改功能 — 一键撤销本轮所有文件变更

#### 🧩 工具管理与系统提示词

- ✨ 实现工具启用/禁用功能及前端管理 — 可在设置中开关任意工具
- ✨ 重构工具系统提示词结构并支持插件安装 — 支持动态加载工具插件
- ✨ 重构侧边栏底部栏并添加工具列表按钮 — 快速浏览所有可用工具
- ✨ 重构系统提示词为协作约定 — 移除冗余工具描述，精简 prompt

#### 🔗 ACP 协议

- ✨ 集成 ACP 协议支持 Agent 通信 — 支持 Agent-to-Agent 通信协议

#### 🎨 欢迎页与上下文指示器

- ✨ 重新设计欢迎页并支持快捷任务启动 — 常见任务一键触发
- ✨ 添加上下文 token 构成估算与展示 — 实时显示系统提示词/历史/工具等各部分 token 占比
- ✨ 优化 token 用量显示 — 支持折叠详情、改进上下文指示器

#### 🌿 会话分支与消息撤回

- ✨ 添加会话分支功能 — 支持从任意消息节点分支新对话
- ✨ 支持无代码快照的消息撤回 — 即使没有 Git 快照也能安全撤回消息
- ✨ 发送消息时即时显示空会话 — 无需等待首次回复即可看到新会话

#### 🐾 宠物管理增强

- ✨ 实现宠物删除功能并更新宠物目录路径 — 支持删除不需要的宠物

#### 🖥️ 全局搜索

- ✨ 全局搜索与多项 UI 交互优化 — 快捷搜索会话、工作区、设置项

#### ⚙️ 模型与配置增强

- ✨ 优化模型客户端并重构前端会话流程 — 更流畅的模型切换体验
- ✨ 添加从 AI 模型复制视觉配置功能 — 一键复制视觉模型配置
- ✨ 增强费用计算并保持一致性 — 更精准的 token 费用统计
- ✨ 添加 AI API 调用日志及优化工具指引注入 — 最近 2 次 API 调用可查
- ✨ 更新 FinishTool 提示语支持 ask_choice 工具 — 更智能的对话终止判断

### Changed

#### 🏗️ 侧边栏重构

- 🔄 支持工作区拖拽排序及持久化 — 拖拽调整工作区顺序，服务端持久保存
- 🔄 简化主题并重构侧边栏交互 — 主题精简，侧边栏操作更直观
- 🔄 优化侧边栏视觉样式与交互反馈 — 选中态、悬停态、拖拽态视觉优化
- 🔄 优化侧边栏与聊天输入组件界面 — 统一输入区域层级

#### 🎨 UI/UX 优化

- 🔄 重构推理强度选择器为滑块交互 — 拖动滑块选择推理深度，体验更直观
- 🔄 将 Git 提交历史改为弹窗并优化右侧面板样式 — 不占用主界面空间
- 🔄 重构项目管理弹窗与消息样式 — 统一弹窗视觉风格
- 🔄 优化技能选择与撤回交互体验 — 搜索效率提升
- 🔄 使用 CSS 变量替换硬编码圆角值 — 统一设计系统
- 🔄 调整标题栏样式与开源社区按钮布局 — 更清晰的导航
- 🔄 调整聊天消息区域的内边距与最大宽度 — 优化阅读体验

#### ⚙️ 代码清理

- 🔄 移除插件及 brainstorm 相关废弃代码 — 清理死代码
- 🔄 使用应用加载完成事件替代 @Init 注解初始化 — 更可靠的服务启动

### Fixed

- 🐛 修复空内容消息序列化及上下文环样式 — 保障消息持久化正确
- 🐛 允许用户选择和复制消息文本 — 修复文本选择被阻止的问题
- 🐛 用户折叠块从悬浮提示改为点击展开 — 修复长消息折叠交互
- 🐛 重置用户中断时同步清除流式中断状态 — 修复中断后无法继续的问题
- 🐛 支持 token 字段并调整提示语优先级 — 兼容不同模型的响应格式
- 🐛 移除连接状态显示并修复空内容判断 — 修复状态显示异常
- 🐛 移除白名单匹配时的冗余状态赋值 — 修复审批状态异常切换
- 🐛 打包时排除 config.json 避免覆盖用户配置 — 修复升级后配置丢失
- 🐛 移除撤销代码重复的错误提示 — 修复重复弹窗

### Chore

- 🔧 升级版本至 v26.7.16

---

## [26.6.29] - 2026-06-29

### Added

#### 🐾 桌面宠物精灵

- ✨ 新增桌面宠物精灵组件 (PetSprite) — 支持精灵图动画（idle/running/waving/jumping/failed 等）、拖拽移动、尺寸调整
- ✨ 新增宠物后端 API (PetController) — 列表、元数据、精灵图、活跃切换、位置/大小持久化
- ✨ 支持多宠物选择与切换 — 设置页面可管理和切换不同宠物
- ✨ 添加宠物初始化功能与快捷安装提示 — 首次使用引导安装
- ✨ config.json 新增 activePet 字段 — 持久化当前活跃宠物

#### 🛠️ 技能选择与权限切换

- ✨ 添加技能选择器 — 聊天输入框内可搜索并多选技能，自动以 `/skill:name` 拼接至消息
- ✨ 添加权限模式切换按钮 — 自由模式/审批模式一键切换，无需进入设置

#### 🔒 HITL 免审批工具

- ✨ HITL 拦截支持免审批工具白名单 — `finish` / `ask_choice` 等纯交互控制流工具直接放行，无需用户确认

#### 🧠 模型适配

- ✨ HttpModelClient 添加 `thinking` 字段支持 — 兼容更多推理模型的 thinking 配置

### Changed

#### 🎨 前端重构

- 🔄 简化前端权限切换 UI — 从设置面板移至输入框，操作更便捷
- 🔄 ChatInput 组件重构 — 集成技能选择器、权限切换、宠物精灵，输入框增加层级控制

### Chore

- 🔧 升级版本至 v26.6.29

---

## [26.6.26] - 2026-06-26

### Added

#### 🤖 视觉工具增强

- ✨ 视觉识别工具新增本地文件路径支持 — 参数名从 imageBase64 改为 image，支持本地文件、URL、Base64 Data URI
- ✨ VisionService 重构支持 ImageBlock — 避免日志泄漏图片内容

#### 💬 聊天交互增强

- ✨ 添加聊天快捷操作按钮 — 常见任务一键触发
- ✨ 添加连接状态显示及 UI 细节改进 — 实时展示服务连接状态

#### 🔄 工作流增强

- ✨ 添加工作流循环控制节点及 DAG 渲染优化 — 支持循环执行、重试等复杂工作流场景

### Changed

#### 🏗️ 代码重构

- 🔄 将 loopra-tool 合并入 loopra-bin — 精简模块结构，减少依赖层级

#### 🎨 侧边栏重构

- 🔄 重设计侧边栏按钮布局，New Session 按钮全宽黑色背景 — 对齐参考 UI 设计
- 🔄 移除折叠按钮，简化标题栏 — 精简侧边栏交互
- 🔄 更新按钮标签和布局，移除 Today 面板和用户信息 — 聚焦核心功能
- 🔄 恢复侧边栏切换和版本控制 — 保留必要的导航功能

#### 🎨 标题栏重构

- 🔄 标题栏对齐参考 UI 设计 — 统一视觉风格

#### 🎨 排版与样式

- 🔄 排版和状态样式对齐参考 UI — 提升视觉一致性
- 🔄 侧边栏新建按钮使用 CSS 变量适配多主题 — 补充暗色与复古主题样式
- 🔄 BlockRenderer 区分推理与工具调用显示 — 优化信息展示

### Fixed

#### 🐛 界面修复

- 🐛 标题栏按钮颜色加深 — 提升可见性
- 🐛 侧边栏按钮标签和颜色修正 — 修复视觉问题
- 🐛 修复侧边栏响应式断点支持 272px 宽度 — 改善窄屏适配

#### 🐛 后端修复

- 🐛 移除多余的分组序列变量，修复分组 ID 生成逻辑 — 修复连续工具调用分组异常
- 🐛 修复 TrimStart 中 BOM 字符的转义语法 — 修复特定环境下的字符串处理问题

### Chore

- 🔧 移除未使用的 TaskItem 组件及导入 — 清理废弃代码
- 🔧 删除旧版打包 CSS — 清理冗余样式文件
- 🔧 测试方法名改为英文，compressJavapOutput 访问权限改为 public — 代码规范化

---

## [26.6.24.1] - 2026-06-24

### Added

#### 🧩 路径组折叠组件

- ✨ 添加路径组折叠组件支持连续推理和工具调用 — 优化长推理链的展示和交互

#### 🔄 工作流增强

- ✨ 增强工作流节点类型和审批机制 — 支持更多节点类型和人工审批流程
- ✨ 工作流自动完成END节点并优化前端面板 — 自动处理工作流结束状态
- ✨ 添加会话工作流API及前端可视化面板 — 可视化展示工作流执行状态
- ✨ 添加工作流DAG工具及前端可视化渲染 — 支持复杂工作流的可视化设计

### Changed

#### 🏗️ 代码重构

- 🔄 移除前端Java检查并优化后端性能与稳定性 — 精简前端依赖，提升后端性能
- 🔄 将工作区选择弹窗样式移至组件内 — 提升组件封装性
- 🔄 移除工作流巡检及自动重试逻辑 — 简化工作流执行逻辑
- 🔄 移除目标(Goal)巡检及相关功能 — 清理废弃功能
- 🔄 移除已废弃的TODO图标及相关逻辑 — 界面精简

#### 🔧 构建与配置

- 🔄 迁移pnpm构建配置并更新gitignore — 优化前端构建流程
- 🔄 使用随机端口替代固定端口 — 避免端口冲突
- 🔄 跳过 Java 检查，由启动脚本处理 — 简化构建过程

### Fixed

#### 🐛 界面与交互修复

- 🐛 助手消息添加文件修改/新增统计 — 准确显示文件变更统计
- 🐛 调整 dock-body 定位与圆角样式 — 修复界面布局问题
- 🐛 完成块样式复用 block-tool，工作流 DAG 使用 CSS 变量 — 统一样式管理
- 🐛 用户长消息添加展开/收起按钮 — 优化长消息阅读体验
- 🐛 路径组内层块使用独立展开状态管理 — 修复展开状态混乱问题
- 🐛 调整路径组折叠组件中步数与路径名的显示顺序 — 优化信息展示逻辑
- 🐛 合并连续assistant消息并修复消息ID生成 — 修复消息显示异常

#### 🐛 工作区与工作流修复

- 🐛 工作流面板仅在会话选中时显示并添加空状态提示 — 优化面板显示逻辑
- 🐛 添加工作区后自动切换到新工作区 — 提升用户体验
- 🐛 修复删除工作区后因副作用被重建的问题 — 修复数据一致性问题

### Chore

- 🔧 删除共享工作区实施计划文档 — 清理临时文档

---

## [26.6.22] - 2026-06-22

### Added

#### 📝 DiffViewer 与文件打开功能

- ✨ 添加文件打开功能并重构 DiffViewer 组件 — 支持在系统编辑器中打开文件
- ✨ 将文件打开按钮移至工具标题行内 — 优化交互布局
- ✨ 无变更时展示文件原文内容 — Diff 为空时仍显示文件内容
- ✨ 将 DiffViewer 遮罩层移至全局样式并更新模型 — 统一样式管理

#### 🛠️ 工具系统增强

- ✨ 连续工具调用合并为分组展示 — 优化多次连续工具调用的 UI 呈现
- ✨ BlockRenderer 展示 bash 命令 — 工具结果中渲染 bash 命令
- ✨ 优化 BlockRenderer 图标样式与 choice 选项展示 — 提升工具卡片视觉体验

#### 🤖 视觉模型与图片识别

- ✨ 添加图片识别服务与视觉模型配置 — 支持 vision_recognize 工具
- ✨ 无视觉模型时自动识别图片并拼接到文本 — 自动处理多模态消息
- ✨ 添加视觉模型远端获取与自动填入功能 — 自动检测可用视觉模型

#### 🧠 模型元数据管理

- ✨ 添加模型元数据管理功能 — 统一管理模型能力、价格等信息
- ✨ 添加模型多模态支持提供者及元数据集成 — 动态获取模型多模态能力
- ✨ 添加模型价格提供者及动态价格计算 — 实时计算 Token 费用
- ✨ 通过 ContextSizeProvider 动态获取模型上下文大小 — 自动适配不同模型
- ✨ 仪表盘显示模型价格提示并清除本地 API 密钥 — 增强安全性与透明度

#### 🔄 工作流增强

- ✨ 添加工作流自动重试与巡检管理功能 — 失败步骤自动重试
- ✨ 添加推理强度切换及运行时热更新 — 运行中可调整推理深度

#### 🔧 其他新增

- ✨ 新增 Windows 打包脚本 — 一键构建 Windows 安装包
- ✨ 更新 AI 工具速查指令为详细参考文档 — 更完善的工具使用指南

### Changed

#### 🏗️ 工具系统重构

- 🔄 将内置工具迁移至 Solon AbsToolProvider 注解驱动模式 — 采用声明式工具注册
- 🔄 迁移工具定义至 Solon 注解并移除旧 ToolDef 体系 — 清理历史代码
- 🔄 移除 ToolDispatcher 与 PlanMode 并重构 ConfigService — 精简架构
- 🔄 精简 ToolContext 及移除未使用的字段与方法 — 减少上下文复杂度
- 🔄 移除沙箱旁路机制及相关参数 — 简化安全模型
- 🔄 适配 solon-ai FunctionTool 并重构工具调用 — 对齐上游 API

#### ⚙️ 代码质量提升

- 🔄 提取 ModelContextUtils 工具类及 Usage 字段常量 — 消除重复代码
- 🔄 提取 JSON 字段为常量以消除硬编码 — 提升可维护性
- 🔄 使用 Lombok 简化静态字段访问并移除冗余缓冲区 — 减少样板代码
- 🔄 统一配置读取方式，移除 sharedConfig 字段 — 消除配置访问不一致

#### 🤖 Agent 架构优化

- 🔄 重构 agent 模块结构，提取 context/loop/model 等子包 — 模块化组织
- 🔄 移除共享 PromptPrefix，每次构建独立初始化 — 避免状态污染
- 🔄 移除 isThinkingMode 及未使用的流式重试逻辑 — 清理死代码
- 🔄 重命名 ChatMessage 工具方法及修复序列化 — 接口规范化
- 🔄 将 usage 去重逻辑从 processChunk 提取到 handleUsage — 职责清晰

#### 🎨 前端样式优化

- 🔄 优化聊天界面布局和样式细节 — 提升视觉一致性
- 🔄 优化移动端响应式布局和样式适配 — 改善移动设备体验
- 🔄 调整消息体最大宽度为 85% — 优化阅读体验
- 🔄 优化 AI 加载动画和助手消息样式 — 增强流式交互感
- 🔄 优化推理文本的列表和引用块样式 — 提升 Markdown 渲染质量
- 🔄 添加消息内链接的代码风格样式 — 统一内联样式
- 🔄 优化停止按钮样式和悬停效果 — 交互反馈增强

### Fixed

- 🐛 修复 ToolContext 序列化与工作目录类型问题 — 保障数据正确持久化
- 🐛 简化 FinishTool 连续未调用提示 — 减少误报干扰
- 🐛 切换模型后调用 loadUsage 刷新用量 — 确保用量数据同步
- 🐛 SSE 流中 usage 去重，仅上报增量 — 防止重复统计
- 🐛 补齐工具消息空内容防御 — 避免空指针异常
- 🐛 兼容 reasoning 字段的解析 — 兼容不同模型的响应格式
- 🐛 恢复无工具调用降级终止逻辑并移除快路径优化 — 保障推理循环稳定性
- 🐛 修复流式 tool_calls 数据累积逻辑 — 修复多轮调用数据丢失
- 🐛 创建快照后恢复用户暂存区状态 — 保障快照操作不影响暂存区

### Chore

- 🔧 升级版本至 v26.6.22
- 🔧 更新依赖并重构组件

---

## [26.6.17.1] - 2026-06-17

### Added

#### 🔔 版本管理与更新

- ✨ 添加核心服务版本落后提示弹窗 — 当核心服务版本落后时显示更新提示
- ✨ 版本信息面板增加自动更新功能 — 支持自动检查和更新版本
- ✨ 通过重定向方式获取最新版本号 — 优化版本获取逻辑
- ✨ 添加桌面端版本管理与更新检查 — 桌面端版本管理功能
- ✨ 支持在线一键安装并优化安装日志显示 — 简化安装流程

#### 🧩 元素面板增强

- ✨ 优化元素检测脚本注入与移除逻辑，增强设计模式状态管理 — 提升元素检测准确性
- ✨ 新增 AI 消息链接悬停浮层与元素界面打开功能 — 增强消息交互体验
- ✨ 优化SSE流错误重试策略并重构元素面板为独立可拖拽组件 — 提升面板稳定性和可用性
- ✨ 添加跨域 iframe 元素检测及修复 web 环境服务连接 — 支持跨域元素检测，修复连接问题

#### 🛠️ 安装器改进

- ✨ 将 Windows 安装器从 Squirrel 替换为 NSIS — 优化安装体验

### Changed

#### 🏗️ 构建系统

- 🔄 升级前端依赖并优化构建配置 — 保持依赖最新
- 🔄 迁移至 electron-builder 并重构 Vite 构建流程 — 优化构建系统
- 🔄 迁移构建系统从 electron-builder 到 electron-forge — 采用更现代的构建工具

#### ⚙️ 代码优化

- 🔄 统一重试逻辑并优化系统提示词管理 — 提升代码可维护性
- 🔄 新增外部链接打开方法并优化标题栏链接交互 — 改进用户交互
- 🔄 隐藏 macOS 原生按钮，优化版本获取重试与显示 — 优化界面一致性

### Fixed

- 🐛 修复 tool_call 参数非 JSON 格式时的处理逻辑 — 增强参数解析鲁棒性
- 🐛 重试机制支持用户中断并统一重试逻辑 — 改善用户体验
- 🐛 仅桌面端显示元素检查按钮并新增外部链接打开方法 — 修复平台兼容性
- 🐛 修复"type":"module"导致的ESM构建报错 — 修复模块导入问题

### Chore

- 🔧 在版本更新脚本中添加 version.txt — 版本管理优化
- 🔧 将版本信息面板抽取为独立组件 — 代码结构优化
- 🔧 提取版本信息面板为独立组件 — 代码结构优化

---

## [26.6.15] - 2026-06-15

### Added

#### 🖥️ Electron 桌面端支持

- ✨ 添加 Electron 桌面端支持，替换 Tauri — 新增 Electron 主进程、预加载脚本、打包配置
- ✨ 添加 `platform.js` 平台适配层 — 替换 `tauri.js`，统一桌面端 API 调用
- ✨ 适配 `SetupScreen`、`SplashScreen`、`TitleBar`、`App.vue` 等组件 — 兼容 Electron 运行环境
- ✨ 添加窗口控制 IPC（最小化/最大化/关闭）与进程树管理（跨平台进程清理）
- ✨ 添加 `electron-builder.json` 打包配置 — 支持 Windows/macOS/Linux 分发
- ✨ 升级 `electron-builder` 依赖并优化构建配置

#### 🧩 元素面板（Element Panel）

- ✨ 新增元素面板（ElementPanel）— 可视化查看页面 DOM 元素树
- ✨ 右侧面板支持拖拽调整宽度 — 灵活的界面布局定制
- ✨ 组件信息改为弹窗展示 — 点击元素查看详细组件信息
- ✨ 支持从元素面板发送消息到当前会话 — 快速引用元素上下文
- ✨ 过滤 FinishTool 提示元素 — 保持面板内容简洁
- ✨ 优化元素面板按钮样式并防止文字换行

#### 🔧 Git 面板增强

- ✨ Git 面板添加模型选择下拉框 — 选择 AI 生成提交消息的模型
- ✨ 添加模型切换警告提示 — 确保用户知晓模型变更的影响
- ✨ `git-author` 配置中添加模型字段 — 持久化用户偏好

#### 🤖 模型与推理优化

- ✨ 简化思考模式判断逻辑 — 减少冗余条件分支
- ✨ 支持模型名后缀配置上下文大小 — 通过模型名称后缀灵活控制上下文窗口

### Changed

- 🔄 统一日志输出，替换多处 `System.err` 为 Slf4j — 提升日志管理一致性（`LoopraConfig`、`HttpModelClient`、`SessionService`、`ToolScanUtil`、`WorkspaceManager`）
- 🔄 简化 `SseAgentOutput` 构造函数 — 减少不必要的参数传递
- 🔄 升级 `electron-builder` 并更新 `pnpm-lock.yaml` 依赖锁定

### Fixed

- 🐛 优化 Electron 进程管理 — 修复窗口关闭时后端进程未正确清理的问题
- 🐛 升级依赖版本 — 修复已知兼容性问题

### Chore

- 📸 添加界面预览图（主界面、设置页、数据面板、Git 面板等 12 张截图）
- 🔧 添加 `bump-version.ps1` 版本更新脚本 — 一键更新版本号

---

## [26.6.14] - 2026-06-14

### Added

#### 📊 数据面板

- ✨ 新增数据面板（Dashboard）— 可视化展示每日 Token 用量、费用统计、模型分布
- ✨ 后端新增 `DashboardService` — 读取 `~/.loopra/usage_daily.jsonl` 并聚合统计数据
- ✨ 新增 `UsageCostCalculator` — 提取 Token 费用计算为独立工具类，供多处复用
- ✨ 前端新增 `Dashboard.vue` 组件 — 支持近 7/14/30 天切换，柱状图 + 模型饼图
- ✨ `SessionStore` 增加每日用量记录接口 — 请求完成后自动追加日志条目
- ✨ 侧边栏新增数据面板入口

### Changed

- 🔄 提取 Token 费用计算逻辑至 `UsageCostCalculator`，消除 `AgentService` 中的重复代码

---

## [26.6.13] - 2026-06-13

### Added

#### 📅 定时任务管理

- ✨ 新增定时任务管理面板 — 可视化管理定时任务，支持会话筛选
- ✨ 优化 Cron 解析器 — 增强表达式解析能力

#### 🎨 前端增强

- ✨ 为 ChatMessage 添加时间戳字段 — 前端显示消息发送时间
- ✨ 添加流式加载动画横线 — 提升流式响应视觉体验
- ✨ 优化 finish 工具渲染 — 区分执行中与完成状态
- ✨ 为 TitleBar 添加 Gitee 和 GitHub Star 按钮 — 方便用户收藏项目

#### 🔄 自动更新系统

- ✨ 新增自动更新功能 — 应用启动时自动检查更新
- ✨ 将自动更新按钮移至更新模态框底部 — 优化交互流程

#### 🤖 子代理增强

- ✨ 传播父级中断信号到子代理 — 确保父级中断时子代理也能正确停止
- ✨ 重构子代理会话渲染，复用 ChatMessage 组件 — 保持UI一致性

#### 📁 Git 增强

- ✨ 支持未跟踪文件的 diff 显示与空文件处理 — 完善Git差异展示

### Changed

- 🔄 更新复古绿主题配色为浅绿 Material Design 风格 — 提升视觉一致性
- 🔄 移除 Chat.vue 中冗余的消息样式代码 — 简化前端代码结构
- 🔄 统一自动更新按钮样式为通用按钮 — 保持UI风格一致
- 🔄 简化会话筛选为“当前会话”与“全部”两个选项 — 降低用户操作复杂度

### Fixed

- 🐛 修复并发写入锁、快照丢失及淘汰不确定性 — 提升系统稳定性

### Performance

- ⚡ 优化 ChatMessage 序列化性能 — 减少不必要的字段序列化

---

## [26.6.12.1] - 2026-06-12

### Added

#### 🔔 版本检查与更新系统

- ✨ 添加版本检查功能 — 自动检测新版本并通知用户
- ✨ 添加版本更新弹窗 — 版本发布时弹出更新提示
- ✨ 添加版本号显示 — 标题栏显示当前版本号
- ✨ 后端新增 `VersionController` 版本检查 API — 支持版本比对与发布信息获取
- ✨ 应用配置 `app.yml` 新增版本检查相关配置项

#### 🛠️ 工具系统

- ✨ `finish` 工具 `content` 参数改为可选 — 简化退出逻辑，调用时无需强制传入内容
- ✨ AgentLoop 增加空值回退处理 — `finish` 无内容时自动生成回退文本，避免空响应

### Changed

- 🔄 前端版本更新弹窗与设置页面整合，位置在 Settings 页面
- 🔄 状态栏增加版本更新状态指示

---

## [26.6.12] - 2026-06-12

### Added

#### 📸 消息快照系统

- ✨ 添加快照检查点与撤回功能 — AI 修改代码前自动基于 Git 创建工作目录快照
- ✨ 撤回快照时同步截断后端会话历史并持久化，前端删除该消息及之后的所有消息
- ✨ 撤回时将已删除的用户消息文本回填到输入框，移除已撤回标记
- ✨ 快照 ID 持久化到会话记录中，支持精确恢复到任意检查点

#### 🤖 推理引擎

- ✨ 集成 Solon AI (solon-ai) LLM 聊天模型 — 重构提交消息生成逻辑，替换原有的硬编码 AI 调用

#### 🎨 前端增强

- ✨ 添加消息缩略图导航栏 — 快速定位和导航历史消息

### Changed

- 🔄 重构 ChatMessage 组件 — 将消息渲染逻辑从 Chat.vue 提取为独立组件，提升可维护性
- 🔄 重构 GitService 服务层 — 增强 Git 操作能力以支撑快照系统
- 🔄 移除 RetryCommand 和 RewindCommand — 被快照撤回功能替代
- 🔄 AgentService 集成 Solon AI — 采用标准化 AI 调用接口

### Performance

- ⚡ 缓存 reasoning 渲染结果 — 避免重复解析推理内容，提升聊天界面滚动性能

### Fixed

- 🐛 移除推理内容区域的 v-html 绑定 — 改用安全渲染方式，修复 XSS 漏洞

---

## [26.6.11.2] - 2026-06-11

### Added

#### 🛠️ 工具系统

- ✨ 添加 Java 源码查找工具 (java_source) — 按全限定类名查找 JDK/Maven 依赖源码
- ✨ 添加 Cron 表达式解析器，支持定时任务调度

#### 🎨 前端增强

- ✨ 添加代码语法高亮 (Shiki/Shikiji) — 支持多种编程语言
- ✨ 添加代码高亮主题切换 — 跟随应用主题自动适配

### Changed

- 🔄 工具调用结果初始展开状态改为折叠 (默认 false)
- 🔄 日志目录迁移至 `~/.loopra/logs`
- 🔄 优化工具调用展开/折叠逻辑及 FinishTool 行为
- 🔄 更新前端构建产物及 Help 组件

### Fixed

- 🐛 修复工作区删除时缓存泄漏及切换时目标目录缺失
- 🐛 补全推理请求体中的 `enable_thinking` 参数，确保推理模型正常启用思考
- 🐛 修复 SSE 流错误响应处理 — 优化错误判断逻辑并格式化响应代码
- 🐛 优化模型选择器刷新逻辑及列表滚动体验
- 🐛 优化 Git 面板滚动区域结构
- 🐛 用户中断时取消正在执行的工具调用，避免僵尸进程

---

## [26.6.11.1] - 2026-06-11

### Added
#### 🎨 UI/UX 增强
- ✨ 应用毛玻璃视觉效果 — 侧边栏、标题栏、状态栏、输入框、卡片、模态框等组件
- ✨ 为浅色、深色、复古等主题新增毛玻璃相关 CSS 变量
- ✨ 调整圆角与阴影变量，增强界面层次感
- ✨ 输入区域固定至底部并调整消息内边距
- ✨ 用量栏合并至输入框底部
- ✨ Diff 预览改为左右对比并 Teleport 至 body
- ✨ 思考和工具卡片添加 Spinner 动画图标
- ✨ 推理卡片默认展开

#### 🔧 Git 集成
- ✨ 增加作者配置弹窗及 Git 配置获取
- ✨ 作者配置持久化改为 API 调用并添加恢复默认功能

#### 🤖 推理引擎
- ✨ 添加 FinishTool 以显式结束推理循环
- ✨ 增加无工具调用降级终止与最大迭代限制

#### 🔗 SSE & 后端适配
- ✨ 适配后端新增 SSE 事件类型（content, tool_call, tool_result）
- ✨ 使用默认会话注册 SSE 确保事件可达

### Changed
- 🔄 移除最大迭代次数限制，改为无限循环
- 🔄 移除 FinishTool content 必填参数，简化退出逻辑
- 🔄 AgentLoop 提示语改为英文
- 🔄 更新默认 Git 作者配置
- 🔄 重构 Git 状态解析及默认作者配置
- 🔄 使用 Slf4j 替换 System.err 输出日志

### Fixed
- 🐛 修复模型列表解析逻辑以兼容新数据结构
- 🐛 修复输入提交后未清空及历史记录未保存的问题
- 🐛 修复终端高度自适应问题

---

## [26.6.10] - 2026-06-10

### Added
#### 🎯 目标系统 (Goal System)
- ✨ 添加 Goal 数据模型层 (Goal, GoalStep, 枚举)
- ✨ 添加 GoalStore 持久化层，支持工作区哈希过滤
- ✨ 添加 GoalEngine 引擎与 GoalPatrolPrompt 巡逻提示
- ✨ 添加 `/goal` 命令处理器 (GoalCommand)
- ✨ 添加会话加载时目标恢复功能
- ✨ 添加 GoalMarkDoneTool — LLM 可主动标记步骤完成
- ✨ 添加 AgentLoop 定时巡逻守护线程 (30s 间隔，并发安全)
- ✨ 添加消息后巡逻触发器与自动重试循环 (失败步骤自动注入上下文重新执行)

#### 🔧 LSP 管理
- ✨ 添加 LSP 服务器管理功能
- ✨ 添加 LSP 完全禁用开关并更新默认配置

#### 📝 Git 集成
- ✨ 添加 AI 自动生成 Git 提交消息功能
- ✨ 添加 Git 提交历史查看功能

#### 🖥️ 界面 & 官网
- ✨ 提取 Sidebar.vue 到独立组件
- ✨ 添加 Loopra 官网终端未来主义风格样式

#### 🔗 工作区与会话
- ✨ 新建会话关联工作区并修复内部类修饰符
- ✨ 切换会话接口支持 workspaceHash 参数

### Changed
- 🔄 统一 LSP 作用域为全局并迁移至 Solon 内置配置
- 🔄 移除 isActive 工作区概念，改用客户端追踪当前会话工作区
- 🔄 移除侧边栏组件及相关状态（重构至独立组件）
- 🔄 重构 LoopraAgent 与 AgentLoop 并增强健壮性
- 🔄 提取硬编码常量并消除重复错误消息
- 🔄 将 Git 业务逻辑抽取至 GitService
- 🔄 使用 Lombok @Getter 替换手写 getter
- 🔄 后端代码优化

### Fixed
- 🐛 修复输入框大小不会自动归位的问题
- 🐛 修复删除会话缺少 workspaceHash 及异常处理优化
- 🐛 移除清除 Agent 时多余的会话追踪清理
- 🐛 严格校验工作区 hash 并优化解析逻辑
- 🐛 Goal 系统：手动序列化 GoalStore、添加清理、修复 TOCTOU
- 🐛 Goal 系统：工作区未初始化时的空安全保护
- 🐛 Goal 系统：代码审查修复 — 添加空守卫、@author 标签、@Builder.Default

### Performance
- ⚡ 优化 Git 提交消息生成延迟及 Token 消耗

---

## [26.6.9.2]

### Added
- ✨ 重构侧边栏为项目-会话两级结构
- ✨ Tauri端使用原生文件夹选择对话框
- ✨ 添加一键展开/折叠所有项目会话
- ✨ 区分无会话与无消息的空状态
- ✨ 添加模型价格配置功能
- ✨ 添加禁用工具设置功能

### Removed
- 🗑️ 移除刷新历史按钮

---

## [26.6.9.1]

### Fixed
- 🐛 修复思考字段未设置，部分模型不启用思考的问题

### Changed
- 🔄 默认上下文设置为 256K

---

## [26.6.8.3]

### Fixed
- 🐛 修复 Mac/Linux 安装脚本失效的问题

---

## [26.6.8]

### Added
- ✨ 首次创建变更日志文件（初版）
